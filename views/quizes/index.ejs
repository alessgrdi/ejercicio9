<table>
  <% var i; for (i=0; i < quizes.length; i++) { %>
    <tr>
      <td><a href="/quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
      <td><label for="resp" class="rem"><%= quizes[i].tema %></label></td>
      <!--<td>
          <form method="post" action="/quizes/<%= quizes[i].id %>?_method=delete">
            <button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">
              borrar
            </button>
          </form>
        </td>-->

<!-- <a href="/quizes/<%= quizes[i].id %>"><%= quizes[i].tema %></a> -->

    </tr>
  <% } %>
</table>
<p>
<a href="/quizes/new"><button>Crear pregunta</button></a>

<br><br>

<label class="rem">Buscar pregunta</label>
<form method="get" action="/quizes/buscar">
  <input type="text" name="search" value=""/>
  <input type="submit" value="Enviar">
</form>
